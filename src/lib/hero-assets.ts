/**
 * Canonical paths for hero-critical imagery — the one place both the
 * `<Image>` elements that render them and anything gating on their load
 * (PremiumLoader today, future scenes later) read from, so the two can
 * never drift out of sync.
 */
export const HERO_VILLA_IMAGE = "/hero-villa.jpg";
export const HERO_MATTRESS_IMAGE = "/images/vexa-one-hero.jpg";
